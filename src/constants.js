const SINGLE_PREFIX = 'single';
const MULTIPLE_PREFIX = 'multiple';

// 轮播图默认配置
const DEFAULT_CONFIG = {
  loop: true,
  fadeEffect: {
    crossFade: true
  }
};

export {
  DEFAULT_CONFIG,
  SINGLE_PREFIX,
  MULTIPLE_PREFIX,
};